// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Added additional burger button functionality and content scrolling."

// Push to remote repo
git push -u origin main

// Pull changes
git pull