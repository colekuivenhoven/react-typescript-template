// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Created individual pages to replace the more general Content page"

// Push to remote repo
git push -u origin main

// Pull changes
git pull