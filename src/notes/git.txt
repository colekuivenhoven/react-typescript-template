// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Fixed route page content border color to respond to theme"

// Push to remote repo
git push -u origin main

// Pull changes
git pull