// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Added chained content cards to the explore page"

// Push to remote repo
git push -u origin main

// Pull changes
git pull