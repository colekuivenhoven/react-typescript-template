// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Updated route pages to be more mobile friendly"

// Push to remote repo
git push -u origin main

// Pull changes
git pull